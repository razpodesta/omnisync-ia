{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "pnpm run internationalization:build && pnpm run persistence:generate && npx nx build admin-dashboard --prod",
  "outputDirectory": "dist/apps/admin-dashboard/.next",
  "installCommand": "pnpm install",
  "env": {
    "NEXT_PUBLIC_API_URL": "https://omnisync-orchestrator.onrender.com",
    "NEXT_INTL_LOCALE": "es"
  }
}
