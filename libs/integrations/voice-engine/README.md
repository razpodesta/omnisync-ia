# voice-engine

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build voice-engine` to build the library.

## Running unit tests

Run `nx test voice-engine` to execute the unit tests via [Jest](https://jestjs.io).
