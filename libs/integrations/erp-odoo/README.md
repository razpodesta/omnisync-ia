# erp-odoo

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build erp-odoo` to build the library.

## Running unit tests

Run `nx test erp-odoo` to execute the unit tests via [Jest](https://jestjs.io).
