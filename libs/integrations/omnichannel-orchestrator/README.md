# omnichannel-orchestrator

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build omnichannel-orchestrator` to build the library.

## Running unit tests

Run `nx test omnichannel-orchestrator` to execute the unit tests via [Jest](https://jestjs.io).
